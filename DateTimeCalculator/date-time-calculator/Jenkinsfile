pipeline {
    agent any 
    tools { 
        maven 'M3' 
        jdk 'JAVA_HOME' 
    }
    stages { 
        stage('Build') { 
            steps {           
                zsh 'mvn clean install'
               
            }
        }
        
    }
}
