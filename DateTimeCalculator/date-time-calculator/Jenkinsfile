pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                withMaven(maven : 'M3') {
                	zsh 'mvn clean install'
                } 
            }
        }
        
    }
}
